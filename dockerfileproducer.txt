# Docker image for springboot file run
# version 0.0.1

# pull base image 基于
from java:8
# listen port 对外暴露端口
expose 20022
# producer 作者
maintainer kuukie <3260681415@qq.com>
# add VRM-VirtualRouter.zip /opt/kuukie/
# copy VirtualNorth.jar /opt/kuukie/
# copy VirtualSouth.jar /opt/kuukie/
# copy virtual-router-close.sh /opt/kuukie/other/
# copy virtual-router-start.sh /opt/kuukie/other/
# copy virtual-start.sh /opt/kuukie/other/
# main java jar 主程序包
copy SpringBootWeb-1.0-SNAPSHOT.jar /opt/kuukie/
# main run 默认启动运行主程序
entrypoint ["java","-jar","/opt/kuukie/SpringBootWeb-1.0-SNAPSHOT.jar"]


/** 
 *  另外一些docker常用命令
 *  docker build -t *** . 执行dockerfile生成tag为***的镜像
 *  docker rm $(docker ps -a -q) 删除docker运行的记录
 *  docker rmi *** 删除镜像为***
 *  docker run -d -p ***:*** *** 运行镜像为***并且绑定端口***:***同时后台运行
 **/
